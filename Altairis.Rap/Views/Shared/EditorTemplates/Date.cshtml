@{
    var value = string.Empty;
    if (ViewData.Model != null) {
        var dtVal = (DateTime)ViewData.Model;
        if (dtVal > DateTime.MinValue) {
            value = dtVal.ToString("yyyy-MM-dd");
        }
    }
}
@Html.TextBox("", value, new { @class = "text-box single-line", type = "date" })

